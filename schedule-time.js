import './time-core.js';

const time=globalThis.__trasyTime;

export const normalizeClockTime=time.normalizeClockTime;
export const rowPlanText=time.rowPlanText;
export const nearestFutureTime=time.nearestFutureTime;
export const addMinutesToTime=time.addMinutesToTime;
export const planDateForRow=time.planDateForRow;
